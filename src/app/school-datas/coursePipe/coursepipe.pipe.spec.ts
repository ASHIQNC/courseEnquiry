import { CoursepipePipe } from './coursepipe.pipe';

describe('CoursepipePipe', () => {
  it('create an instance', () => {
    const pipe = new CoursepipePipe();
    expect(pipe).toBeTruthy();
  });
});
